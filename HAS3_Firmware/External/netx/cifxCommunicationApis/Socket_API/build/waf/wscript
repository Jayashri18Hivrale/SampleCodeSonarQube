def build(bld):

    includes = [
    "./../../include",
    ]
    
    bld.sdkcomponent(
        name            = "SocketDefinitions_sdk",
        uses            = "cifXToolkit_sdk",
        export_includes = includes,
    )