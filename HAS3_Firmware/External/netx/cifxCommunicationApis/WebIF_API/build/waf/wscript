def build(bld):

    includes = [
    "./../../include",
    ]
    
    bld.sdkcomponent(
        name            = "WebIfDefinitions_sdk",
        uses            = "cifXToolkit_sdk",
        export_includes = includes,
    )